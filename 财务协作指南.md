# 💰 财务 × 财务机器人 · 人机协作指南

> 给财务同学的操作手册。一句话定位：**机器人负责收票、识别、记账、催收、出材料；你负责核对、录入学校系统、把报销办成。**
> 所有机器人指令都在**审批群**里 @机器人 发送；所有台账都在**审批多维表格**里。

---

## 一、人机分工总览（先看这张表）

| 环节 | 机器人自动做 | 你做 |
|------|------------|------|
| 收票归档 | 私聊收队员发票 → 识别 → 归类到申请 → 给队员回执 → 写入台账 | 基本不用管 |
| 催收 | 每天自动私聊催超期，交了票的自动停催 | 周一看周报掌握全貌 |
| 防错 | 重复发票拦截、抬头校验、发票金额 vs 申请金额比对 | 只处理它标出来的例外 |
| 出材料 | 一键生成 打印件 PDF（按录入顺序一页两票）+ BOM 表 | 检查后打印使用 |
| 录入学校系统 | —— | 小翼Plus 逐张扫码（机器人给逐张核对清单） |
| 报销记账 | 报一句话它记账、改状态 | submit / paid / reject 一句话 |

---

## 二、平时：队员交票（全自动，你只管例外）

**队员怎么交**：把发票直接**私聊机器人**（催办私信里也可以直接回图）——支持数电票 PDF、二维码截图、拍照。机器人自动识别、查重、校验抬头、比对金额，归到对应采购申请，并给队员发回执。队员交了票就自动移出催办名单，不用再改审批。

**你只需要处理机器人标出来的例外**：在多维表格「**发票采集**」表里看「校验状态」列，不是「通过」的：

| 状态 | 意思 | 你的动作 |
|------|------|---------|
| 待人工 | 没归上类（名下多条申请金额对不上等） | 人工填「关联申请编号」列 |
| 金额不符 | 发票金额和申请金额差超 ±5%/¥10 | 看备注里的差额，找队员确认原因 |
| 抬头存疑 | 购买方不是重庆大学/校税号 | 让队员重开票 |

识别失败的照片会被机器人直接打回给队员（附重发指引），不用你转达；实在识别不了的纸质票，你手工录入小翼Plus 即可，或人工在采集表补一行备查。

---

## 三、报销：三件套流程（核心场景）

以「给步兵组报销一批」为例：

### 第 0 步 · 看拟批
审批群 @机器人 发：
```
/approval-batch
```
机器人按项目分组列出票池：每个项目多少张、多少钱、有几张异常。

### 第 1 步 · 锁定批次
```
/approval-batch lock 27备赛20步兵5 步兵机器人
```
- 批次号沿用咱们自己的命名习惯（`27备赛20步兵5` 这样），项目名照拟批列表填；**不填项目 = 全部票池锁定**。
- 锁定后机器人自动做三件事：①这批票回写审批表「报销单」栏；②生成**打印件 PDF**（严格按录入顺序、A4 竖版一页两票）；③生成 **BOM 表**（物资/型号/金额/发票对照+合计）。
- 两个文件都在多维表格「**报销批次**」表对应行的「打印文件」「BOM表」附件里，下载即用。
- ⚠️ **锁定后顺序不可变**（打印件顺序 = 扫码顺序）。要补票只能进下一批；锁错了没提交的可以 `reject` 回池重来。

### 第 2 步 · 录入学校系统
打开打印件，按顺序逐张用小翼Plus 扫码录入重大财务系统。机器人拟批清单和打印件顺序完全一致，每张票的 20 位发票号码 / 开票日期 / 价税合计都能对上，照单核对即可。

### 第 3 步 · 提交 / 到账 / 退回
```
/approval-batch submit 27备赛20步兵5    ← 学校提交成功
/approval-batch paid    27备赛20步兵5    ← 钱到账
/approval-batch reject  27备赛20步兵5    ← 被学校退回（这批票自动回票池，重新拟批）
```
附：打印件或 BOM 生成失败了？`/approval-batch regen 27备赛20步兵5` 重新生成。随时看全部批次：`/approval-batch status`。

---

## 四、台账与周报

- **两张台账表**（审批多维表格里）：「发票采集」= 每张票的明细（发票号/金额/归属申请/校验状态/批次/票面图片）；「报销批次」= 每批的张数/金额/状态/打印件/BOM。
- **每周一 18:00 周报**卡末尾有「报销台账」段：票池待归集多少张多少钱、各状态批次一览。平时不用主动查，看周报就知道工作区状态。

---

## 五、常见问题

**Q1 队员说交了票，但审批表里没看到？**
先看「发票采集」表：有记录 = 只是「补交发票」栏镜像没同步，不影响，以采集表为准；没记录 = 他发错地方了，让他私聊机器人重发。

**Q2 机器人的「疑似重复」拦错了？**
拼单各交各的票确实会撞查重。你在采集表人工补一行（发票号+归属申请），并备注说明。

**Q3 批次锁定后发现混进了不该有的票？**
没提交学校之前：`reject` 整批回池 → 重新拟批锁定。

**Q4 队员交的是纸质票？**
机器人只收电子件（PDF/图片）。纸票你直接录小翼Plus，不需要走机器人。

**Q5 忘了指令怎么写？**
发 `/approval-help` 看全部财务指令；`/approval-batch` 不带参数会回操作提示。

---

## 六、机器人的边界（这些仍然是你的活）

- **学校系统操作必须人工**：小翼Plus 扫码查验、网页端填报、打印装订、线下提交——机器人不碰学校系统；
- **最终核对责任在人**：批次提交前扫一眼 BOM 合计和打印件张数，机器人给的清单是核对工具，不是免检通行证；
- **例外判断靠人**：机器人只负责把「金额不符/抬头存疑/待人工」标出来并提醒队员，真正的沟通和处理由你决定。

> 遇到机器人行为异常（识别离谱、回执没发、指令报错），截图 + 一句话说明反馈给机器人管理员即可。

*指南对应机器人版本：approval-bot v46（2026-09-25）。功能更新会随周报/群公告同步。*
